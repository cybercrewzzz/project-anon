import React from "react";
import { View, Text } from "react-native";

const peertopeer = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text style={{ fontSize: 24, fontWeight: "bold" }}>Peer to Peer Screen</Text>
            <Text style={{ fontSize: 16, marginTop: 10 }}>This is where the peer to peer functionality will be implemented.</Text>
        </View>
    );
}

export default peertopeer;